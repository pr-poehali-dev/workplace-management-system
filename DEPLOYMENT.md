# Руководство по развертыванию на VPS

## Введение

Этот проект можно развернуть на любом VPS-сервере или хостинге с SSH доступом. Система автоматически создаст все необходимые конфигурационные файлы для быстрого развертывания.

---

## Способ 1: Автоматическое развертывание (рекомендуется)

### Шаг 1: Подготовка конфигурации

1. Войдите в админ-панель проекта
   - Логин: `admin`
   - Пароль: `admin123`

2. Перейдите в **Администрирование → Развертывание**

3. Заполните форму:
   ```
   Адрес сервера: 192.168.1.100 (или your-domain.com)
   SSH порт: 22
   Пользователь SSH: root
   Пароль SSH: ваш_пароль
   Путь на сервере: /var/www/html
   ```

4. Нажмите **"Начать развертывание"**

5. Система скачает 4 файла:
   - `deploy.sh` - bash скрипт для автоматической установки
   - `nginx.conf` - готовая конфигурация Nginx
   - `docker-compose.yml` - конфигурация Docker Compose
   - `README.md` - инструкция по установке

### Шаг 2: Скачивание билда проекта

1. В редакторе poehali.dev нажмите **Скачать → Скачать билд**
2. Сохраните архив `project-build.zip`

### Шаг 3: Загрузка на сервер

#### Через SCP (рекомендуется):

```bash
# Распакуйте билд локально
unzip project-build.zip -d build

# Загрузите файлы на сервер
scp -r build/* root@your-server:/var/www/html/
scp deploy.sh nginx.conf docker-compose.yml root@your-server:/var/www/html/
```

#### Через FTP/SFTP:

1. Подключитесь к серверу через FileZilla или другой FTP-клиент
2. Загрузите содержимое папки `build` в `/var/www/html/`
3. Загрузите `deploy.sh`, `nginx.conf` в ту же директорию

### Шаг 4: Запуск развертывания

```bash
# Подключитесь к серверу
ssh root@your-server

# Перейдите в директорию проекта
cd /var/www/html

# Сделайте скрипт исполняемым
chmod +x deploy.sh

# Запустите развертывание
sudo ./deploy.sh
```

Скрипт автоматически:
- Установит Node.js (если не установлен)
- Установит Nginx (если не установлен)
- Настроит конфигурацию Nginx
- Создаст необходимые директории
- Покажет инструкции для финальной настройки

### Шаг 5: Финальная настройка

После выполнения скрипта следуйте инструкциям на экране:

```bash
# Активируйте конфигурацию Nginx
sudo ln -s /etc/nginx/sites-available/myproject /etc/nginx/sites-enabled/

# Проверьте конфигурацию
sudo nginx -t

# Перезапустите Nginx
sudo systemctl restart nginx
```

### Шаг 6: Настройка приложения

1. Откройте браузер и перейдите по адресу вашего сервера
2. Войдите как администратор (admin / admin123)
3. Перейдите в **Администрирование → Настройки VPS**
4. Введите адрес API: `http://your-server` или `http://192.168.1.100`
5. Нажмите **"Проверить соединение"**
6. Нажмите **"Сохранить настройки"**

✅ Готово! Приложение развернуто и настроено.

---

## Способ 2: Docker развертывание (самый быстрый)

### Требования:
- Docker
- Docker Compose

### Инструкция:

```bash
# 1. Загрузите файлы проекта и docker-compose.yml на сервер

# 2. Запустите контейнеры
docker-compose up -d

# 3. Проверьте статус
docker-compose ps

# 4. Просмотрите логи (при необходимости)
docker-compose logs -f
```

Приложение будет доступно на порту 80.

---

## Способ 3: Ручная настройка Nginx

### 1. Установка Nginx

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install nginx -y

# CentOS/RHEL
sudo yum install nginx -y
```

### 2. Создание конфигурации

Скопируйте содержимое скачанного `nginx.conf` или создайте вручную:

```bash
sudo nano /etc/nginx/sites-available/myproject
```

Вставьте конфигурацию (см. nginx.conf из скачанных файлов)

### 3. Активация конфигурации

```bash
sudo ln -s /etc/nginx/sites-available/myproject /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

### 4. Настройка файрвола

```bash
# Откройте порт 80
sudo ufw allow 80/tcp
sudo ufw reload
```

---

## Настройка SSL (HTTPS)

### Использование Let's Encrypt (бесплатно)

```bash
# Установите Certbot
sudo apt install certbot python3-certbot-nginx -y

# Получите сертификат
sudo certbot --nginx -d your-domain.com

# Автоматическое обновление
sudo certbot renew --dry-run
```

После получения сертификата обновите настройки в админ-панели:
- API URL: `https://your-domain.com`

---

## Troubleshooting

### Проблема: Nginx не запускается

**Решение:**
```bash
# Проверьте конфигурацию
sudo nginx -t

# Проверьте логи
sudo tail -f /var/log/nginx/error.log

# Проверьте, свободен ли порт 80
sudo netstat -tlnp | grep :80
```

### Проблема: Не загружаются данные

**Решение:**
1. Проверьте настройки API в админ-панели
2. Откройте консоль браузера (F12) → вкладка Network
3. Проверьте, что запросы идут на правильный адрес
4. Убедитесь, что backend функции развернуты и доступны

### Проблема: Ошибки прав доступа

**Решение:**
```bash
# Установите правильные права
sudo chown -R www-data:www-data /var/www/html
sudo chmod -R 755 /var/www/html
```

### Проблема: 502 Bad Gateway

**Решение:**
1. Проверьте, что backend сервер запущен
2. Проверьте настройки proxy_pass в nginx.conf
3. Убедитесь, что порт backend доступен

---

## Обновление проекта

Для обновления проекта:

1. Скачайте новый билд из редактора
2. Подключитесь к серверу
3. Создайте резервную копию:
   ```bash
   sudo cp -r /var/www/html /var/www/html.backup
   ```
4. Загрузите новые файлы:
   ```bash
   scp -r build/* root@your-server:/var/www/html/
   ```
5. Перезапустите Nginx:
   ```bash
   sudo systemctl restart nginx
   ```

---

## Мониторинг и логи

### Логи Nginx

```bash
# Логи доступа
sudo tail -f /var/log/nginx/myproject_access.log

# Логи ошибок
sudo tail -f /var/log/nginx/myproject_error.log
```

### Статус системы

```bash
# Статус Nginx
sudo systemctl status nginx

# Использование диска
df -h

# Использование памяти
free -h

# Запущенные процессы
htop
```

---

## Безопасность

### Рекомендации:

1. **Измените пароль администратора** сразу после первого входа
2. **Используйте SSH ключи** вместо паролей
3. **Настройте firewall**:
   ```bash
   sudo ufw enable
   sudo ufw allow 22/tcp
   sudo ufw allow 80/tcp
   sudo ufw allow 443/tcp
   ```
4. **Регулярно обновляйте систему**:
   ```bash
   sudo apt update && sudo apt upgrade -y
   ```
5. **Используйте HTTPS** (SSL сертификаты)

---

## Дополнительные ресурсы

- [Документация Nginx](https://nginx.org/ru/docs/)
- [Docker документация](https://docs.docker.com/)
- [Let's Encrypt](https://letsencrypt.org/)
- [Digital Ocean туториалы](https://www.digitalocean.com/community/tutorials)

---

## Поддержка

Если возникли проблемы:

1. Проверьте логи (см. раздел "Мониторинг и логи")
2. Откройте консоль браузера (F12) для проверки ошибок
3. Обратитесь в сообщество: https://t.me/+QgiLIa1gFRY4Y2Iy

---

**Успешного развертывания! 🚀**
