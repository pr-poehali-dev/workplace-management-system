# Быстрый старт

## Вход в систему

**Учетные данные администратора по умолчанию:**
- Логин: `admin`
- Пароль: `admin123`

## Развертывание на VPS/Хостинге (НОВОЕ! 🚀)

### Автоматическое развертывание

1. Войдите в админ-панель
2. Перейдите в "Администрирование" → "Развертывание"
3. Заполните данные SSH:
   - Адрес сервера (IP или домен)
   - Порт SSH (обычно 22)
   - Пользователь (обычно root)
   - Пароль
   - Путь установки (например /var/www/html)
4. Нажмите **"Начать развертывание"**
5. Система скачает 4 файла:
   - `deploy.sh` - скрипт автоматической установки
   - `nginx.conf` - конфигурация веб-сервера
   - `docker-compose.yml` - для Docker
   - `README.md` - подробная инструкция

### Загрузка на сервер

```bash
# 1. Скачайте билд: "Скачать → Скачать билд" в редакторе
# 2. Загрузите на сервер:
scp -r build/* root@your-server:/var/www/html/
scp deploy.sh root@your-server:/var/www/html/

# 3. Подключитесь и запустите:
ssh root@your-server
cd /var/www/html
chmod +x deploy.sh
sudo ./deploy.sh
```

## Настройка для работы на VPS (быстрый способ)

Если проект уже развернут:

1. Нажмите на иконку пользователя в правом верхнем углу
2. Выберите "Администрирование"
3. Перейдите на вкладку "Настройки VPS"
4. Нажмите **"Авто"** для автоопределения
5. Нажмите **"Проверить соединение"** 
6. Нажмите **"Сохранить настройки"**

Готово! Сайт настроен для работы на вашем сервере.

## Изменение пароля администратора

1. Откройте "Администрирование"
2. Вкладка "Учетные данные"
3. Введите текущий пароль: `admin123`
4. Укажите новый логин и/или пароль
5. Сохраните изменения

⚠️ **Важно:** Сохраните новые учетные данные в надежном месте!

## Дополнительная информация

Подробное руководство: `vps-setup-guide.md`